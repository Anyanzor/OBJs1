import * as moduloMatematicas from './modulos/controller.js';

const sum = moduloMatematicas.suma(4, 3)
console.log(sum)

const mul = moduloMatematicas.multiplica(2, 2)
import chalk from 'chalk';
console.log(chalk.green(mul));
